# Validation Report — enablement-live-debugger-bug-hunting

| Field | Value |
|---|---|
| Date | 2026-05-25 |
| Training ID | `live-debugger` |
| Job ID | `enablement-722df34ac6e7` |
| Pages crawled | 15 |
| Steps executed | 125 |
| Pass | 2 |
| Fail | **2** |
| Expected-fail | 0 |
| Skip | 121 |
| Duration | 817s |

## Summary

**2 step(s) failed.** See details below.

## Step Findings

### 1. [⏭️ SKIP] `ui` step

```
1.1 Enable Observability for Developers #
```

**Error:** UI/verify steps skipped (--no-ui)

### 2. [⏭️ SKIP] `ui` step

```
Go to Settings > Collect and Capture > Observability for Developers > Enable Observability for Developers
```

**Error:** UI/verify steps skipped (--no-ui)

### 3. [⏭️ SKIP] `ui` step

```
Go to Settings > Collect and Capture > General monitoring settings > OneAgent features. Enable the Java Live-Debugger, Node.js Live-Debugger, or both, depending on your needs.
```

**Error:** UI/verify steps skipped (--no-ui)

### 4. [⏭️ SKIP] `ui` step

```
Enable the Java Live-Debugger, Node.js Live-Debugger, or both, depending on your needs.
```

**Error:** UI/verify steps skipped (--no-ui)

### 5. [⏭️ SKIP] `ui` step

```
1.2 Set IAM Policies #
```

**Error:** UI/verify steps skipped (--no-ui)

### 6. [⏭️ SKIP] `ui` step

```
We take security very seriously. So let's create a policy to set user-level Live Debugging breakpoints.
```

**Error:** UI/verify steps skipped (--no-ui)

### 7. [⏭️ SKIP] `ui` step

```
We need two policies for your user to be able to set breakpoints and to read snapshots. 
For this we go to Account Management > Identity & Access management > + Policy
```

**Error:** UI/verify steps skipped (--no-ui)

### 8. [⏭️ SKIP] `ui` step

```
Go to Group Management > Select Admin Group > + Permission (and bind the policy)
```

**Error:** UI/verify steps skipped (--no-ui)

### 9. [⏭️ SKIP] `ui` step

```
Like this you have the fine control to give your developers, SRE teams and whoever you want to set breakpoints and read the snapshots. For more granular access please continue reading here
```

**Error:** UI/verify steps skipped (--no-ui)

### 10. [⏭️ SKIP] `ui` step

```
1.3 Enable Live Debugger ActiveGate module #
```

**Error:** UI/verify steps skipped (--no-ui)

### 11. [⏭️ SKIP] `ui` step

```
Now we need to enable the Live Debugger ActiveGate module :
```

**Error:** UI/verify steps skipped (--no-ui)

### 12. [⏭️ SKIP] `ui` step

```
1.4 Enable Log Ingest #
```

**Error:** UI/verify steps skipped (--no-ui)

### 13. [⏭️ SKIP] `ui` step

```
Enable built-in log ingest rule to ingest all logs discovered by Dynatrace
```

**Error:** UI/verify steps skipped (--no-ui)

### 14. [⏭️ SKIP] `ui` step

```
Enable log/trace enrichment
```

**Error:** UI/verify steps skipped (--no-ui)

### 15. [⏭️ SKIP] `ui` step

```
Go to Settings > General monitoring settings > OneAgent features.
```

**Error:** UI/verify steps skipped (--no-ui)

### 16. [⏭️ SKIP] `ui` step

```
Enable: Java - Trace/span context enrichment for logs [Opt-In]. Java - Trace/span context enrichment for unstructured logs [Opt-In]
```

**Error:** UI/verify steps skipped (--no-ui)

### 17. [⏭️ SKIP] `ui` step

```
2. Create Dynatrace API Tokens for Kubernetes Observability #
```

**Error:** UI/verify steps skipped (--no-ui)

### 18. [⏭️ SKIP] `ui` step

```
Open the Kubernetes App (CTRL + K then type Kubernetes for fast access)
```

**Error:** UI/verify steps skipped (--no-ui)

### 19. [⏭️ SKIP] `ui` step

```
Select the + Add cluster button
```

**Error:** UI/verify steps skipped (--no-ui)

### 20. [⏭️ SKIP] `ui` step

```
Click on generate Token for the 'Dynatrace Operator' and save it to your Notepad
```

**Error:** UI/verify steps skipped (--no-ui)

### 21. [⏭️ SKIP] `ui` step

```
Click on generate Token for the 'Data Ingest Token' and save it to your Notepad
```

**Error:** UI/verify steps skipped (--no-ui)

### 22. [⏭️ SKIP] `ui` step

```
Now we are ready to launch the Codespace! You'll need your tenant and the two tokens previuosly gathered from above. When you enter the tenant please enter it without the 'apps' part, for production t
```

**Error:** UI/verify steps skipped (--no-ui)

### 23. [⏭️ SKIP] `ui` step

```
Branch select the main branch
```

**Error:** UI/verify steps skipped (--no-ui)

### 24. [⏭️ SKIP] `ui` step

```
select the main branch
```

**Error:** UI/verify steps skipped (--no-ui)

### 25. [⏭️ SKIP] `ui` step

```
Machine sizing As a machine type select 4-core
```

**Error:** UI/verify steps skipped (--no-ui)

### 26. [⏭️ SKIP] `ui` step

```
As a machine type select 4-core
```

**Error:** UI/verify steps skipped (--no-ui)

### 27. [⏭️ SKIP] `ui` step

```
Secrets (enter your credentials within the following variables) DT_ENVIRONMENT DT_OPERATOR_TOKEN DT_INGEST_TOKEN
```

**Error:** UI/verify steps skipped (--no-ui)

### 28. [⏭️ SKIP] `ui` step

```
2. While the Codespace is set-up for you, learn powerful usecases for Live Debugging #
```

**Error:** UI/verify steps skipped (--no-ui)

### 29. [⏭️ SKIP] `ui` step

```
While the Codespace is being set up for you, here is a 6 minute read about powerful use cases beyond live debugging in our Dynatrace blog.
```

**Error:** UI/verify steps skipped (--no-ui)

### 30. [⏭️ SKIP] `ui` step

```
The app TODO app is being exposed from the devcontainer to your localhost or the github dns domain. If you want to make the endpoints public accesible, just go to the ports section in VsCode, right cl
```

**Error:** UI/verify steps skipped (--no-ui)

### 31. [✅ PASS] `shell` step

```
kubectl cluster-info
```

### 32. [✅ PASS] `shell` step

```
showOpenPorts(){
  sudo netstat -tulnp
}
```

### 33. [⏭️ SKIP] `ui` step

```
Open the TODO App and add a couple of tasks #
```

**Error:** UI/verify steps skipped (--no-ui)

### 34. [⏭️ SKIP] `verify` step

```
In VSCode open a new terminal and in the Welcome Message you should see a link to the Todo App UI. Click on it. Now add a couple of tasks.
It should look something like this:
```

**Error:** UI/verify steps skipped (--no-ui)

### 35. [⏭️ SKIP] `ui` step

```
Add a couple of tasks.
```

**Error:** UI/verify steps skipped (--no-ui)

### 36. [⏭️ SKIP] `verify` step

```
If you want to verify you have bumped correctly into the bug, you can verify this with a function we set up for you.
```

**Error:** UI/verify steps skipped (--no-ui)

### 37. [⏭️ SKIP] `verify` step

```
🪲 Verify if you have bumped into the bug
```

**Error:** UI/verify steps skipped (--no-ui)

### 38. [⏭️ SKIP] `ui` step

```
Click Here to continue the quest
```

**Error:** UI/verify steps skipped (--no-ui)

### 39. [⏭️ SKIP] `ui` step

```
Open the Kubernetes App > You'll see a cluster with the name "codespace-xxxx". That's your Kind cluster being fully observed.
```

**Error:** UI/verify steps skipped (--no-ui)

### 40. [⏭️ SKIP] `ui` step

```
On the right hand side, click on Workloads, the Workloads Page will open.
```

**Error:** UI/verify steps skipped (--no-ui)

### 41. [⏭️ SKIP] `ui` step

```
On the right hand side, click on Workloads, the Workloads Page will open.
```

**Error:** UI/verify steps skipped (--no-ui)

### 42. [⏭️ SKIP] `ui` step

```
This will open all the traces that went through your application... wow!
```

**Error:** UI/verify steps skipped (--no-ui)

### 43. [⏭️ SKIP] `ui` step

```
Now we know which method and which package! Let's put our Developer Hat 🎩 and open the Live Debugger.
```

**Error:** UI/verify steps skipped (--no-ui)

### 44. [⏭️ SKIP] `ui` step

```
Open Live Debugger #
```

**Error:** UI/verify steps skipped (--no-ui)

### 45. [⏭️ SKIP] `ui` step

```
Once you click next, no repository was found for this application (this is because no repo was configured). Let's add it manually.
```

**Error:** UI/verify steps skipped (--no-ui)

### 46. [⏭️ SKIP] `ui` step

```
Click on the + sign to add a new one
```

**Error:** UI/verify steps skipped (--no-ui)

### 47. [⏭️ SKIP] `ui` step

```
Click on the + sign to add a new one
```

**Error:** UI/verify steps skipped (--no-ui)

### 48. [⏭️ SKIP] `ui` step

```
Click on authenticate. It'll open a window where you'll authenticate with your GitHub personal account.
```

**Error:** UI/verify steps skipped (--no-ui)

### 49. [⏭️ SKIP] `ui` step

```
Click on authenticate. It'll open a window where you'll authenticate with your GitHub personal account.
```

**Error:** UI/verify steps skipped (--no-ui)

### 50. [⏭️ SKIP] `ui` step

```
Click on Done!
```

**Error:** UI/verify steps skipped (--no-ui)

### 51. [⏭️ SKIP] `verify` step

```
Now you are all set! you should see an  image like this:
```

**Error:** UI/verify steps skipped (--no-ui)

### 52. [⏭️ SKIP] `ui` step

```
Navigate to the TodoController.java #
```

**Error:** UI/verify steps skipped (--no-ui)

### 53. [⏭️ SKIP] `ui` step

```
You can set a non-breaking breakpoint by clicking just to the left of the line number.  Set it and wait for the status to change to Active .
```

**Error:** UI/verify steps skipped (--no-ui)

### 54. [⏭️ SKIP] `ui` step

```
Return to the Live Debugger and see the Snaphot captured, open it and see all the variables that were captured with their values.
```

**Error:** UI/verify steps skipped (--no-ui)

### 55. [⏭️ SKIP] `ui` step

```
Did you notice? With Dynatrace we were able to navigate from the Kubernetes Cluster all the way down to the workload, it's traces cotinuing down to the specific method and namespace of the called func
```

**Error:** UI/verify steps skipped (--no-ui)

### 56. [⏭️ SKIP] `verify` step

```
Is the bug gone? Open the app and verify it!
```

**Error:** UI/verify steps skipped (--no-ui)

### 57. [⏭️ SKIP] `ui` step

```
Go to the terminal and type:
```

**Error:** UI/verify steps skipped (--no-ui)

### 58. [⏭️ SKIP] `ui` step

```
Click here to continue the quest with the next Bug
```

**Error:** UI/verify steps skipped (--no-ui)

### 59. [⏭️ SKIP] `ui` step

```
Add a new Task #
```

**Error:** UI/verify steps skipped (--no-ui)

### 60. [⏭️ SKIP] `ui` step

```
Add a task with an exclamation mark! or a special character for that matter, like this:
```

**Error:** UI/verify steps skipped (--no-ui)

### 61. [⏭️ SKIP] `ui` step

```
Now type ENTER to add the task
```

**Error:** UI/verify steps skipped (--no-ui)

### 62. [⏭️ SKIP] `ui` step

```
Click here to Continue the quest
```

**Error:** UI/verify steps skipped (--no-ui)

### 63. [⏭️ SKIP] `ui` step

```
Open the Distributed Tracing App #
```

**Error:** UI/verify steps skipped (--no-ui)

### 64. [⏭️ SKIP] `ui` step

```
Protip: Open the Tracing App anywhere in Dynatrace
```

**Error:** UI/verify steps skipped (--no-ui)

### 65. [⏭️ SKIP] `ui` step

```
Type CTRL + K, then search for 'Tracing'. The Tracing App should appear in the Super Search.
```

**Error:** UI/verify steps skipped (--no-ui)

### 66. [⏭️ SKIP] `ui` step

```
In the filter add:
```

**Error:** UI/verify steps skipped (--no-ui)

### 67. [⏭️ SKIP] `ui` step

```
Open the Live Debugger #
```

**Error:** UI/verify steps skipped (--no-ui)

### 68. [⏭️ SKIP] `ui` step

```
Let's add two breakpoints around that line, one before, let's say on line 23 and another on line 30.
```

**Error:** UI/verify steps skipped (--no-ui)

### 69. [⏭️ SKIP] `ui` step

```
Go to the TodoApp in your browser and add a Task with a special character.
```

**Error:** UI/verify steps skipped (--no-ui)

### 70. [⏭️ SKIP] `verify` step

```
Is the bug gone? Open the app and verify it!
```

**Error:** UI/verify steps skipped (--no-ui)

### 71. [⏭️ SKIP] `ui` step

```
Go to the terminal and type:
```

**Error:** UI/verify steps skipped (--no-ui)

### 72. [⏭️ SKIP] `ui` step

```
Click here to continue the quest with the next Bug
```

**Error:** UI/verify steps skipped (--no-ui)

### 73. [⏭️ SKIP] `ui` step

```
Open the TODO App and add a Task #
```

**Error:** UI/verify steps skipped (--no-ui)

### 74. [⏭️ SKIP] `ui` step

```
Add a Task that we want to duplicate, we add Call the Bugbusters
```

**Error:** UI/verify steps skipped (--no-ui)

### 75. [⏭️ SKIP] `ui` step

```
Click here to continue the quest
```

**Error:** UI/verify steps skipped (--no-ui)

### 76. [⏭️ SKIP] `ui` step

```
Open the Logs App. Again let's be a pro and type CTRL + K and then type in Logs, the Logs App should appear in the super search.
```

**Error:** UI/verify steps skipped (--no-ui)

### 77. [⏭️ SKIP] `ui` step

```
To view the related distributed trace, we can either:
- Right click on the Log line and open record with and you select the Distributed Tracing app
- On the right details pane, above the Topology sect
```

**Error:** UI/verify steps skipped (--no-ui)

### 78. [⏭️ SKIP] `ui` step

```
Open the Live Debugger #
```

**Error:** UI/verify steps skipped (--no-ui)

### 79. [⏭️ SKIP] `ui` step

```
Go to the Todo app and repeat the bug.
```

**Error:** UI/verify steps skipped (--no-ui)

### 80. [⏭️ SKIP] `ui` step

```
Return to the Live Debugger session and click on the new snapshot.  Review the variables.
```

**Error:** UI/verify steps skipped (--no-ui)

### 81. [⏭️ SKIP] `ui` step

```
Looking at the source code, in lines 100 and 101, the variables are incorrectly set because the functions are swapped!
```

**Error:** UI/verify steps skipped (--no-ui)

### 82. [⏭️ SKIP] `verify` step

```
Is the bug gone? Open the app and verify it!
```

**Error:** UI/verify steps skipped (--no-ui)

### 83. [⏭️ SKIP] `ui` step

```
Go to the terminal and type:
```

**Error:** UI/verify steps skipped (--no-ui)

### 84. [⏭️ SKIP] `ui` step

```
Click here to continue
```

**Error:** UI/verify steps skipped (--no-ui)

### 85. [⏭️ SKIP] `verify` step

```
We recommend to automate the version control integration within the build and deployment process of the applications. This way, when a developer debugs an application in whatever cluster and environme
```

**Error:** UI/verify steps skipped (--no-ui)

### 86. [⏭️ SKIP] `ui` step

```
Below is a sample bash script that patches the Kubernetes deployment of the todo application. It'll read the revision for v1.0.0 and the remote URL of the repository and set them accordingly as enviro
```

**Error:** UI/verify steps skipped (--no-ui)

### 87. [❌ FAIL] `shell` step

```
#!/bin/bash

# Small bash script for patching deployments via kubectl, more information about integrating the live debugger with your version control see: 
# https://docs.dynatrace.com/docs/observe/ap
```

**Error:** fatal: ambiguous argument 'v1.0.1': unknown revision or path not in the working tree.
Use '--' to separate paths from revisions, like this:
'git <command> [<revision>...] -- [<file>...]'
Error from se

**Stdout:**
```
Fetching git revision for v1.0.1 in https://github.com/dynatrace-wwse/enablement-live-debugger-bug-hunting.git
v1.0.1

```

### 88. [❌ FAIL] `shell` step

```
bash app/patches/set_version_control.sh
```

**Error:** fatal: ambiguous argument 'v1.0.1': unknown revision or path not in the working tree.
Use '--' to separate paths from revisions, like this:
'git <command> [<revision>...] -- [<file>...]'
Error from se

**Stdout:**
```
Fetching git revision for v1.0.1 in https://github.com/dynatrace-wwse/enablement-live-debugger-bug-hunting.git
v1.0.1

```

### 89. [⏭️ SKIP] `verify` step

```
Verify that the container has the version control information #
```

**Error:** UI/verify steps skipped (--no-ui)

### 90. [⏭️ SKIP] `verify` step

```
We can easily verify that the pod has the environment variables set. With the help of k9s we can easily navigate and describe the deployment.
```

**Error:** UI/verify steps skipped (--no-ui)

### 91. [⏭️ SKIP] `verify` step

```
Open the Live Debugger and verify that the Version Control is setted automatically #
```

**Error:** UI/verify steps skipped (--no-ui)

### 92. [⏭️ SKIP] `ui` step

```
Go to Dynatrace and open the Live Debugger.
```

**Error:** UI/verify steps skipped (--no-ui)

### 93. [⏭️ SKIP] `ui` step

```
In case you have an old session, click on "New Session"
```

**Error:** UI/verify steps skipped (--no-ui)

### 94. [⏭️ SKIP] `verify` step

```
Select the "todoapp" container and verify that the hash matches the actual POD that has been re-deployed after the patch.
```

**Error:** UI/verify steps skipped (--no-ui)

### 95. [⏭️ SKIP] `ui` step

```
After you click on "Next" the repository should be fetched automatically like the following screenshot matching the remote URL and the revision ID.
```

**Error:** UI/verify steps skipped (--no-ui)

### 96. [⏭️ SKIP] `ui` step

```
Click here to continue
```

**Error:** UI/verify steps skipped (--no-ui)

### 97. [⏭️ SKIP] `ui` step

```
On the top left, click on the VS Code menu (the 3 lines), and then click on "Open in VS Code Desktop". This will launch your local VS Code IDE. If you don't have VS Code already installed in your comp
```

**Error:** UI/verify steps skipped (--no-ui)

### 98. [⏭️ SKIP] `ui` step

```
Now your Codespace is open inside your VS Code IDE. You'll notice that you have now the full IDE experience, you can drag and drop files inside and outside the IDE from your OS and VSCode will automat
```

**Error:** UI/verify steps skipped (--no-ui)

### 99. [⏭️ SKIP] `ui` step

```
On the left pane, click on "Extensions", then search for "Dynatrace" > Select "Observability for Developers" and click on "Install in Codespaces"
```

**Error:** UI/verify steps skipped (--no-ui)

### 100. [⏭️ SKIP] `ui` step

```
Select the stage you want to connect #
```

**Error:** UI/verify steps skipped (--no-ui)

### 101. [⏭️ SKIP] `ui` step

```
Great! now that you have the Dynatrace extension installed in your IDE. Unless you are using a DEV or Hardening (sprint) tenant, this configuration is not needed since th default is "production". All 
```

**Error:** UI/verify steps skipped (--no-ui)

### 102. [⏭️ SKIP] `ui` step

```
The extension settings will open. You can select the environment you want to connect. As options you'll get:
```

**Error:** UI/verify steps skipped (--no-ui)

### 103. [⏭️ SKIP] `ui` step

```
Select the desired environment and that's it. You can select it for the User, Remote CS instance and for the Workspace.
```

**Error:** UI/verify steps skipped (--no-ui)

### 104. [⏭️ SKIP] `ui` step

```
Now let's login to the environment using SSO. On the bottom pane, there is a tab called "Dynatrace Snapshots", open it.  Click on the button "Log in".
```

**Error:** UI/verify steps skipped (--no-ui)

### 105. [⏭️ SKIP] `ui` step

```
This will open a browser and will pass on the SSO credentials to your IDE via http://localhost:3232. When the browser tab opens, enter your credentials, login and accept the pop up to connect back to 
```

**Error:** UI/verify steps skipped (--no-ui)

### 106. [⏭️ SKIP] `ui` step

```
If after you enter your login credentials in the browser, the extensions keeps on loading, just restart the extension so it can authenticate properly. Passing in the credentials from the Browser to th
```

**Error:** UI/verify steps skipped (--no-ui)

### 107. [⏭️ SKIP] `ui` step

```
Select the tenant ☁️ #
```

**Error:** UI/verify steps skipped (--no-ui)

### 108. [⏭️ SKIP] `ui` step

```
Since we are using the comfort of SSO, you can select any tenant you have permission on the logged in production or whatever environment you previously selected. For selecting a specific tenant click 
```

**Error:** UI/verify steps skipped (--no-ui)

### 109. [⏭️ SKIP] `ui` step

```
Select a running container ✏️ #
```

**Error:** UI/verify steps skipped (--no-ui)

### 110. [⏭️ SKIP] `ui` step

```
Set a non-breaking breakpoint #
```

**Error:** UI/verify steps skipped (--no-ui)

### 111. [⏭️ SKIP] `ui` step

```
Click here to continue
```

**Error:** UI/verify steps skipped (--no-ui)

### 112. [⏭️ SKIP] `ui` step

```
Dynatrace provides you with tools that enable you to meet your data protection and other compliance requirements while still getting value from any data collected by Dynatrace, including the Live Debu
```

**Error:** UI/verify steps skipped (--no-ui)

### 113. [⏭️ SKIP] `ui` step

```
In your Dynatrace tenant go to:
```

**Error:** UI/verify steps skipped (--no-ui)

### 114. [⏭️ SKIP] `ui` step

```
Rule type we select "Redact by variable name" and enter "title"
```

**Error:** UI/verify steps skipped (--no-ui)

### 115. [⏭️ SKIP] `ui` step

```
Data replacement we select "Replace with string" and leave the default value ***
```

**Error:** UI/verify steps skipped (--no-ui)

### 116. [⏭️ SKIP] `verify` step

```
Verify that the "title" is being masked #
```

**Error:** UI/verify steps skipped (--no-ui)

### 117. [⏭️ SKIP] `ui` step

```
Once you have applied the masking rule, open the "Todo app" and add a new task, or remove it, depending the Method where you have the Live Debbugging breakpoint.
```

**Error:** UI/verify steps skipped (--no-ui)

### 118. [⏭️ SKIP] `ui` step

```
Click here to continue
```

**Error:** UI/verify steps skipped (--no-ui)

### 119. [⏭️ SKIP] `ui` step

```
Another way to do this is by going to https://github.com/codespaces and delete the codespace.
```

**Error:** UI/verify steps skipped (--no-ui)

### 120. [⏭️ SKIP] `ui` step

```
You may also want to deactivate or delete the API tokens needed for this lab.
```

**Error:** UI/verify steps skipped (--no-ui)

### 121. [⏭️ SKIP] `ui` step

```
Create a Free Trial in Dynatrace
```

**Error:** UI/verify steps skipped (--no-ui)

### 122. [⏭️ SKIP] `ui` step

```
Or open an issue to share suggestions, topics you'd like us to cover, or examples you'd find helpful.
```

**Error:** UI/verify steps skipped (--no-ui)

### 123. [⏭️ SKIP] `ui` step

```
Thank you for helping us create better enablement resources for everyone ❤️
```

**Error:** UI/verify steps skipped (--no-ui)

### 124. [⏭️ SKIP] `verify` step

```
Stay tuned, more enablements are coming whith more advanced usecases, where we can: hunt bugs from Real User monitoring watch variables in deep nested in objects use the Live Debugger plugin so you ne
```

**Error:** UI/verify steps skipped (--no-ui)

### 125. [⏭️ SKIP] `verify` step

```
fix code on the fly and verify that the bugs are gone
```

**Error:** UI/verify steps skipped (--no-ui)
