# Validation Report — enablement-workflow-essentials

| Field | Value |
|---|---|
| Date | 2026-05-25 |
| Training ID | `workflow-essentials` |
| Job ID | `enablement-3b7abd619236` |
| Pages crawled | 7 |
| Steps executed | 76 |
| Pass | 0 |
| Fail | **23** |
| Expected-fail | 0 |
| Skip | 53 |
| Duration | 163s |

## Summary

**23 step(s) failed.** See details below.

## Step Findings

### 1. [⏭️ SKIP] `ui` step

```
In your Dynatrace environment, open the Workflows App.
```

**Error:** UI/verify steps skipped (--no-ui)

### 2. [⏭️ SKIP] `verify` step

```
Validate that the following permissions are granted to workflow executions.
```

**Error:** UI/verify steps skipped (--no-ui)

### 3. [⏭️ SKIP] `ui` step

```
[ x ] select all
```

**Error:** UI/verify steps skipped (--no-ui)

### 4. [⏭️ SKIP] `ui` step

```
In your Dynatrace environment, search, locate, and open the Hub App.
```

**Error:** UI/verify steps skipped (--no-ui)

### 5. [⏭️ SKIP] `ui` step

```
Click on Install to install the App into your environment.
```

**Error:** UI/verify steps skipped (--no-ui)

### 6. [⏭️ SKIP] `verify` step

```
Validate that the App was installed into your environment.
```

**Error:** UI/verify steps skipped (--no-ui)

### 7. [⏭️ SKIP] `ui` step

```
If you do not already have a Slack App and token, follow the documentation steps to create one.
```

**Error:** UI/verify steps skipped (--no-ui)

### 8. [⏭️ SKIP] `ui` step

```
In your Dynatrace environment, open the Workflows App.
```

**Error:** UI/verify steps skipped (--no-ui)

### 9. [⏭️ SKIP] `verify` step

```
Run the workflow and validate that it runs successfully.
```

**Error:** UI/verify steps skipped (--no-ui)

### 10. [⏭️ SKIP] `verify` step

```
The allow list for outbound connections has been updated with the hosts/domains required for this lab No action required, simply verify using screenshot below
```

**Error:** UI/verify steps skipped (--no-ui)

### 11. [⏭️ SKIP] `verify` step

```
No action required, simply verify using screenshot below
```

**Error:** UI/verify steps skipped (--no-ui)

### 12. [⏭️ SKIP] `verify` step

```
No action required, simply verify using screenshot below
```

**Error:** UI/verify steps skipped (--no-ui)

### 13. [⏭️ SKIP] `ui` step

```
In your Dynatrace environment, open the Workflows App.
```

**Error:** UI/verify steps skipped (--no-ui)

### 14. [⏭️ SKIP] `ui` step

```
Set the task name and description respectively.
```

**Error:** UI/verify steps skipped (--no-ui)

### 15. [❌ FAIL] `shell` step

```
export default async function () {

  let PARAMETERS = {

    // INITIALS - the initials of the user for this lab
    INITIALS: "", // i.e. INITIALS: "TPC"; the intials of the user for this lab are TP
```

**Error:** sh: 1: Syntax error: "(" unexpected


### 16. [⏭️ SKIP] `ui` step

```
Set the value of the missing variables:
```

**Error:** UI/verify steps skipped (--no-ui)

### 17. [⏭️ SKIP] `ui` step

```
WEBHOOK_URL: Navigate in your incognito browser window to https://webhook.site/ and copy your unique URL The unique URL is located at the top of the page, not in your browser's address bar
```

**Error:** UI/verify steps skipped (--no-ui)

### 18. [⏭️ SKIP] `ui` step

```
Navigate in your incognito browser window to https://webhook.site/ and copy your unique URL
```

**Error:** UI/verify steps skipped (--no-ui)

### 19. [⏭️ SKIP] `ui` step

```
Set the task name and description respectively.
```

**Error:** UI/verify steps skipped (--no-ui)

### 20. [❌ FAIL] `shell` step

```
import { execution } from '@dynatrace-sdk/automation-utils';

const PARAMETERS_TASK = 'js_set_parameters';

export default async function ({ execution_id }) {

  // get parameters from previous tasks

```

**Error:** sh: 1: import: not found
sh: 3: const: not found
sh: 5: Syntax error: "(" unexpected


### 21. [⏭️ SKIP] `verify` step

```
Run the workflow and validate the results
```

**Error:** UI/verify steps skipped (--no-ui)

### 22. [⏭️ SKIP] `ui` step

```
Set the task name and description respectively.
```

**Error:** UI/verify steps skipped (--no-ui)

### 23. [❌ FAIL] `shell` step

```
import { execution } from '@dynatrace-sdk/automation-utils';

const PARAMETERS_TASK = 'js_set_parameters';

export default async function ({ execution_id }) {

  // get parameters from previous tasks

```

**Error:** sh: 1: import: not found
sh: 3: const: not found
sh: 5: Syntax error: "(" unexpected


### 24. [⏭️ SKIP] `ui` step

```
Set the task name and description respectively.
```

**Error:** UI/verify steps skipped (--no-ui)

### 25. [❌ FAIL] `shell` step

```
import { execution } from '@dynatrace-sdk/automation-utils';

const PARAMETERS_TASK = 'js_set_parameters';
const FETCH_STATUS_TASK = 'js_fetch_status_io';

export default async function ({ execution_i
```

**Error:** sh: 1: import: not found
sh: 3: const: not found
sh: 4: const: not found
sh: 6: Syntax error: "(" unexpected


### 26. [⏭️ SKIP] `verify` step

```
Run the workflow and validate the results
```

**Error:** UI/verify steps skipped (--no-ui)

### 27. [⏭️ SKIP] `ui` step

```
Set the task name and description respectively.
```

**Error:** UI/verify steps skipped (--no-ui)

### 28. [⏭️ SKIP] `ui` step

```
Set the task name and description respectively.
```

**Error:** UI/verify steps skipped (--no-ui)

### 29. [❌ FAIL] `shell` step

```
import { execution, actionExecution } from '@dynatrace-sdk/automation-utils';
import { getCurrentUserDetails,getEnvironmentId,getEnvironmentUrl } from '@dynatrace-sdk/app-environment';

const PARAMETE
```

**Error:** sh: 1: import: not found
sh: 2: import: not found
sh: 4: const: not found
sh: 6: Syntax error: "(" unexpected


### 30. [❌ FAIL] `shell` step

```
// import the required package
import { actionExecution } from '@dynatrace-sdk/automation-utils';
// declare function
export default async function ({ execution_id, action_execution_id }) {
// get the
```

**Error:** sh: 1: //: Permission denied
sh: 2: import: not found
sh: 3: //: Permission denied
sh: 4: Syntax error: "(" unexpected


### 31. [⏭️ SKIP] `verify` step

```
Run the workflow and validate the results
```

**Error:** UI/verify steps skipped (--no-ui)

### 32. [⏭️ SKIP] `ui` step

```
In the next section, we'll learn to ingest data and create alerts.
```

**Error:** UI/verify steps skipped (--no-ui)

### 33. [⏭️ SKIP] `ui` step

```
Set Workflow Parameters #
```

**Error:** UI/verify steps skipped (--no-ui)

### 34. [⏭️ SKIP] `ui` step

```
Set the task name and description respectively.
```

**Error:** UI/verify steps skipped (--no-ui)

### 35. [❌ FAIL] `shell` step

```
export default async function () {

  let PARAMETERS = {

    // EASYTRAVEL_URL - the URL to the easytravel rest API to search for journeys and receive JSON payload
    EASYTRAVEL_URL: "https://raw.gi
```

**Error:** sh: 1: Syntax error: "(" unexpected


### 36. [⏭️ SKIP] `ui` step

```
Set the value of the missing variables:
```

**Error:** UI/verify steps skipped (--no-ui)

### 37. [⏭️ SKIP] `ui` step

```
Set the task name and description respectively.
```

**Error:** UI/verify steps skipped (--no-ui)

### 38. [⏭️ SKIP] `verify` step

```
Run the workflow and validate the results
```

**Error:** UI/verify steps skipped (--no-ui)

### 39. [⏭️ SKIP] `ui` step

```
Set the task name and description respectively.
```

**Error:** UI/verify steps skipped (--no-ui)

### 40. [❌ FAIL] `shell` step

```
import { execution } from '@dynatrace-sdk/automation-utils';
import { credentialVaultClient } from '@dynatrace-sdk/client-classic-environment-v2';

const PARAMETERS_TASK = 'js_set_parameters';

export
```

**Error:** sh: 1: import: not found
sh: 2: import: not found
sh: 4: const: not found
sh: 6: Syntax error: "(" unexpected


### 41. [⏭️ SKIP] `ui` step

```
Set the task name and description respectively.
```

**Error:** UI/verify steps skipped (--no-ui)

### 42. [⏭️ SKIP] `verify` step

```
Run the workflow and validate the results
```

**Error:** UI/verify steps skipped (--no-ui)

### 43. [⏭️ SKIP] `ui` step

```
Set the task name and description respectively.
```

**Error:** UI/verify steps skipped (--no-ui)

### 44. [❌ FAIL] `shell` step

```
import { execution } from '@dynatrace-sdk/automation-utils';
import { metricsClient } from "@dynatrace-sdk/client-classic-environment-v2";

const PARAMETERS_TASK = 'js_set_parameters';
const SEARCH_TA
```

**Error:** sh: 1: import: not found
sh: 2: import: not found
sh: 4: const: not found
sh: 5: const: not found
sh: 7: Syntax error: "(" unexpected


### 45. [⏭️ SKIP] `verify` step

```
Run the workflow and validate the results
```

**Error:** UI/verify steps skipped (--no-ui)

### 46. [⏭️ SKIP] `ui` step

```
When data is ingested into the Dynatrace platform, it may not be available immediately.  To avoid a query from returning an empty result set, we'll wait 30 seconds for the data to be processed before 
```

**Error:** UI/verify steps skipped (--no-ui)

### 47. [⏭️ SKIP] `ui` step

```
Set the task name and description respectively.
```

**Error:** UI/verify steps skipped (--no-ui)

### 48. [❌ FAIL] `shell` step

```
export default async function () {

  const sleep = async (waitTime: number) =>
  new Promise(resolve =>
    setTimeout(resolve, waitTime));

  const waitTime = async () => {
  // 30,000ms = 30 second
```

**Error:** sh: 1: Syntax error: "(" unexpected


### 49. [❌ FAIL] `shell` step

```
import { execution } from '@dynatrace-sdk/automation-utils';
import { queryExecutionClient } from "@dynatrace-sdk/client-query";
import { eventsClient, EventIngestEventType } from "@dynatrace-sdk/clie
```

**Error:** sh: 1: import: not found
sh: 2: import: not found
sh: 3: import: not found
sh: 5: const: not found
sh: 6: const: not found
sh: 8: Syntax error: "(" unexpected


### 50. [⏭️ SKIP] `verify` step

```
Run the workflow and validate the results
```

**Error:** UI/verify steps skipped (--no-ui)

### 51. [⏭️ SKIP] `ui` step

```
In the next section, we'll set up third party integration.
```

**Error:** UI/verify steps skipped (--no-ui)

### 52. [⏭️ SKIP] `ui` step

```
Set Workflow Parameters #
```

**Error:** UI/verify steps skipped (--no-ui)

### 53. [⏭️ SKIP] `ui` step

```
Set the task name and description respectively.
```

**Error:** UI/verify steps skipped (--no-ui)

### 54. [❌ FAIL] `shell` step

```
export default async function () {

  let PARAMETERS = {

    // SLACK_CHANNEL - the Slack channel name (without the #) that the Slack bot has been added to
    SLACK_CHANNEL: "", // i.e. SLACK_CHANNE
```

**Error:** sh: 1: Syntax error: "(" unexpected


### 55. [⏭️ SKIP] `ui` step

```
Set the value of the missing variables:
- SLACK_CHANNEL:
    * Use the channel name you have been provided (or have created)
```

**Error:** UI/verify steps skipped (--no-ui)

### 56. [⏭️ SKIP] `ui` step

```
Set the task name and description respectively.
```

**Error:** UI/verify steps skipped (--no-ui)

### 57. [⏭️ SKIP] `verify` step

```
Open the Workflow Essentials - Ingest and Alert workflow from the previous exercise.  Run the workflow and validate the results.  A new problem event should have been created.
```

**Error:** UI/verify steps skipped (--no-ui)

### 58. [⏭️ SKIP] `verify` step

```
Shortly after running the other workflow, the Workflow Essentials - Slack Integration workflow should have triggered as a result of the problem event.  Check the execution and validate the results.  O
```

**Error:** UI/verify steps skipped (--no-ui)

### 59. [⏭️ SKIP] `ui` step

```
Workflow Parameters: Set Parameter Task #
```

**Error:** UI/verify steps skipped (--no-ui)

### 60. [⏭️ SKIP] `ui` step

```
The first task of a workflow should be a parameter task that defines the parameters/variables used by the remainder of tasks in the workflow.  Environment and deployment unique settings, like URLs, en
```

**Error:** UI/verify steps skipped (--no-ui)

### 61. [❌ FAIL] `shell` step

```
export default async function () {

  let PARAMETERS = {
    // PARAMETER - description of the parameter
    PARAMETER: "VALUE", // i.e. PARAMETER: "VALUE" - example value of the parameter
  }

  retu
```

**Error:** sh: 1: Syntax error: "(" unexpected


### 62. [❌ FAIL] `shell` step

```
import { execution } from '@dynatrace-sdk/automation-utils';

const PARAMETERS_TASK = 'js_set_parameters';

export default async function ({ execution_id }) {

  // get parameters from previous tasks

```

**Error:** sh: 1: import: not found
sh: 3: const: not found
sh: 5: Syntax error: "(" unexpected


### 63. [❌ FAIL] `shell` step

```
import { getCurrentUserDetails } from '@dynatrace-sdk/app-environment';
import { getEnvironmentId } from '@dynatrace-sdk/app-environment';
import { getEnvironmentUrl } from '@dynatrace-sdk/app-environ
```

**Error:** sh: 1: import: not found
sh: 2: import: not found
sh: 3: import: not found
sh: 5: //: Permission denied
sh: 6: import: not found
sh: 8: Syntax error: "(" unexpected


### 64. [❌ FAIL] `shell` step

```
import { execution, actionExecution } from '@dynatrace-sdk/automation-utils';

export default async function ({ execution_id, action_execution_id }) {

  // execution
  const ex = await execution(exec
```

**Error:** sh: 1: import: not found
sh: 3: Syntax error: "(" unexpected


### 65. [❌ FAIL] `shell` step

```
export default async function () {

  const sleep = async (waitTime: number) =>
  new Promise(resolve =>
    setTimeout(resolve, waitTime));

  const waitTime = async () => {
  // 30,000ms = 30 second
```

**Error:** sh: 1: Syntax error: "(" unexpected


### 66. [⏭️ SKIP] `verify` step

```
For HTTPS calls, the URL/domain/host must have a valid signed SSL certificate or the call will fail.  There is no way to bypass the SSL certificate check due to data privacy and security standards.
```

**Error:** UI/verify steps skipped (--no-ui)

### 67. [❌ FAIL] `shell` step

```
export default async function () {

  // set the fetch() api parameters [method, headers, body]
  // https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API/Using_Fetch
  const options = {
    meth
```

**Error:** sh: 1: Syntax error: "(" unexpected


### 68. [⏭️ SKIP] `verify` step

```
For HTTPS calls, the URL/domain/host must have a valid signed SSL certificate or the call will fail.  There is no way to bypass the SSL certificate check due to data privacy and security standards.
```

**Error:** UI/verify steps skipped (--no-ui)

### 69. [❌ FAIL] `shell` step

```
export default async function () {

  // set the fetch() api parameters [method, headers, body]
  // https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API/Using_Fetch
  const json_body = {
    ke
```

**Error:** sh: 1: Syntax error: "(" unexpected


### 70. [❌ FAIL] `shell` step

```
import { queryExecutionClient } from "@dynatrace-sdk/client-query";

export default async function () {

  // build the DQL query
  // https://developer.dynatrace.com/develop/sdks/client-query/#querye
```

**Error:** sh: 1: import: not found
sh: 3: Syntax error: "(" unexpected


### 71. [❌ FAIL] `shell` step

```
import { credentialVaultClient } from '@dynatrace-sdk/client-classic-environment-v2';

export default async function () {

  // CREDENTIAL - the credential vault entity ID
  const CREDENTIAL = "CREDEN
```

**Error:** sh: 1: import: not found
sh: 3: Syntax error: "(" unexpected


### 72. [❌ FAIL] `shell` step

```
import { metricsClient } from "@dynatrace-sdk/client-classic-environment-v2";

export default async function () {

  // build the line_protocol string to ingest the metric data point
  const line_prot
```

**Error:** sh: 1: import: not found
sh: 3: Syntax error: "(" unexpected


### 73. [❌ FAIL] `shell` step

```
import { eventsClient, EventIngestEventType } from "@dynatrace-sdk/client-classic-environment-v2";

export default async function () {

  // https://developer.dynatrace.com/develop/sdks/client-classic
```

**Error:** sh: 1: import: not found
sh: 3: Syntax error: "(" unexpected


### 74. [⏭️ SKIP] `ui` step

```
Disable or delete the Workflows in your tenant.
```

**Error:** UI/verify steps skipped (--no-ui)

### 75. [⏭️ SKIP] `ui` step

```
Or open an issue to share suggestions, topics you'd like us to cover, or examples you'd find helpful.
```

**Error:** UI/verify steps skipped (--no-ui)

### 76. [⏭️ SKIP] `ui` step

```
Thank you for helping us create better enablement resources for everyone ❤️
```

**Error:** UI/verify steps skipped (--no-ui)
