# Orbital configuration inventory

Generated by `tools/inventory/orbital-inventory.sh`. Values of secrets are
never included — only their names. Deterministic: safe to diff run-to-run.

- host: `ip-172-31-36-172`
- environment: `prod`

## Systemd units (enabled state)

```
nginx.service                            enabled
oauth2-proxy.service                     enabled
ops-dashboard.service                    enabled
ops-docker-cleanup.service               disabled
ops-docker-cleanup.timer                 enabled
ops-gen2scan.service                     disabled
ops-gen2scan.timer                       disabled
ops-nightly.service                      disabled
ops-nightly.timer                        enabled
ops-sync-daemon.service                  enabled
ops-sync-daemon.timer                    disabled
ops-webhook.service                      enabled
ops-worker.service                       disabled
orbital-backup.service                   disabled
orbital-backup.timer                     enabled
orbital-metrics.service                  static
orbital-metrics.timer                    enabled
orbital-restore-drill.service            disabled
orbital-restore-drill.timer              enabled
redis-server.service                     enabled
redis-server@.service                    disabled
```

## Services expected to be running

```
nginx                active
redis-server         active
oauth2-proxy         active
ops-dashboard        active
ops-webhook          active
ops-worker           active
```

## Listening sockets (service-owned)

```
22       sshd
53       systemd-resolve
80       nginx
443      nginx
4180     oauth2-proxy
6379     redis-server
8080     python
8443     python
8787     python3
```

## nginx server names

```
autonomous-enablements.whydevslovedynatrace.com
~^(?<app_subdomain>[^.]+)\.autonomous-enablements\.whydevslovedynatrace\.com$
~^[^.]+\.autonomous-enablements\.whydevslovedynatrace\.com$
```
http2 directives on listeners (must be **0** — nginx 1.24 lacks RFC 8441, breaks WebSocket):
```
0
```

## TLS certificates

```
all certs valid for >30 days: yes
```
<!-- volatile:begin -->
```
autonomous-enablements.whydevslovedynatrace.com         expires Nov 24 09:23:47 2026 GMT
```
<!-- volatile:end -->
certbot authenticator:
```
authenticator = manual
manual_auth_hook = /etc/letsencrypt/hooks/gcp-dns-auth.sh
```

## SSH

```
port(s):              22
passwordauthentication: no
permitrootlogin:      without-password
```

## Redis

```
appendonly           yes
appendfsync          everysec
save                 3600 1 300 100 60 10000
maxmemory            0
maxmemory-policy     noeviction
bind                 127.0.0.1 172.31.36.172
protected-mode       yes
auth-passwords       1
```

## Secret names held in /home/ops/.env

Count and names only. A new name appearing here without a corresponding
change in git is worth a question.
```
ANTHROPIC_API_KEY
APP_DEPLOY_REF
CODESPACES_TRACKER_TOKEN
COE_CLIENT_ID
COE_CLIENT_SECRET
COE_MASTER_CLIENT_ID
COE_MASTER_CLIENT_SECRET
COE_RESOURCE
COE_TENANT_URL
CONTENT_KEYS
CONTENT_STATE_DIR
CONTROL_LOOP_APPLY
DT_ACCOUNT_API_TOKEN
DT_ACCOUNT_URN
DT_ENVIRONMENT
DT_INGEST_TOKEN
DT_LLM_TOKEN
DT_MASTER_PLATFORM_TOKEN
DT_MINT_TOKEN
DT_MINT_TOKEN_SPRINT
DT_OPERATOR_TOKEN
DT_PLATFORM_TOKEN
ENDPOINT_CODESPACES_TRACKER
GH_DEPLOY_TOKEN
GH_OAUTH_ENC_KEY
GH_TOKEN
GH_TOKEN_TTL
GITHUB_OAUTH_CLIENT_ID
GITHUB_OAUTH_CLIENT_SECRET
MAX_PARALLEL_AGENTS
MAX_PARALLEL_WORKERS
MINT_API_HOST_SPRINT
MINT_CLIENT_ID_SPRINT
MINT_CLIENT_SECRET_SPRINT
MINT_RESOURCE_SPRINT
MINT_SSO_SPRINT
OAUTH2_CLIENT_ID
OAUTH2_CLIENT_SECRET
OAUTH2_COOKIE_SECRET
OAUTH2_GITHUB_ORG
OPS_FLEET_OWNERS
OPS_HOST
OPS_PORT
ORBITAL_PUBLIC_URL
ORBITAL_TOKEN
REDIS_PASSWORD
REDIS_URL
REMOTE_GRAIL_COE_TOKEN_ENC
SPRINT_CLIENT_ID
SPRINT_CLIENT_SECRET
SPRINT_NARROW_CLIENT_ID
SPRINT_NARROW_CLIENT_SECRET
SPRINT_RESOURCE
SRO_ACCOUNT_URN
SRO_CLIENT_ID
SRO_CLIENT_SECRET
SRO_MASTER_PLATFORM_TOKEN
SRO_MINTER_PLATFORM_TOKEN
SRO_NARROW_CLIENT_ID
SRO_NARROW_CLIENT_SECRET
SRO_OAUTH_CLIENT_ID
SRO_OAUTH_CLIENT_SECRET
SRO_PLATFORM_TOKEN
SRO_QUICKSTART_PLATFORM_TOKEN
SRO_RESOURCE
SRO_TENANT_URL
TRAINING_TEST_API_TOKEN
TRAINING_TEST_REQUIRE_MINT
TRAINING_TEST_TENANT_URL
WEBHOOK_SECRET
```

## Pinned versions

```
APP_DEPLOY_REF       v1.0.352
CONTROL_LOOP_APPLY   1
WORKER_CODE_BRANCH   (unset → main)
```

## Fleet code refs

The invariant is that every host reports the SAME ref — not any particular
ref, which changes on every legitimate promotion. The verdict below is what
--check compares; the refs themselves are marked volatile and excluded, so a
normal deploy does not register as drift and train everyone to ignore it.
```
all hosts on the same ref: yes
hosts reporting:           3
```
<!-- volatile:begin -->
```
master (ip-172-31-36-172)                  a0e9424
autonomous-enablements-worker              a0e9424
autonomous-enablements-worker-2            a0e9424
```
<!-- volatile:end -->

## OneAgent

Prod workers must be **infra-only**: host and container metrics plus
container logs, with no injection into learner containers.
```
HOST                                       SERVICE    MODE
master (ip-172-31-36-172)                  active     infra-only
autonomous-enablements-worker              inactive   fullstack
autonomous-enablements-worker-2            inactive   none
```

## Backups

```
backup timer             enabled
drill timer              enabled
offsite S3               NOT CONFIGURED — local only
snapshot <48h old        yes
```
